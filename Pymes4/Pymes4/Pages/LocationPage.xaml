<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Pymes4.Pages.LocationPage"
             Title="Ubicación"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">
    <ContentPage.Content>
        <ScrollView 
            BindingContext="{Binding LocationPage}">
            <StackLayout Padding="10">
                <Button Text="Proceder con..." Command="{Binding OpenMapsCommand}"/>
                <Button Text="Proceder con Waze"  Command="{Binding OpenWazeCommand}"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>